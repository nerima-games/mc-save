{
  // Editor / language-server default project. CI gates use tsconfig.build.json
  // (shipped source), tsconfig.test.json (tests + tooling) and
  // tsconfig.browser.json (Chromium IndexedDB contract) directly; this file
  // exists so that opening any file in an editor resolves against the same
  // options as tsconfig.test.json.
  "extends": "./tsconfig.test.json"
}
